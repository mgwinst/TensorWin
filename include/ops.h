#pragma once

enum class Op {
    STORE = 1,
    ADD = 2,
    SUB = 3,
    MUL = 4,
    DIV = 5,
    REDUCE = 6,
    SUM = 7,
    MATMUL = 8,
    CONV = 9
};

enum class UnaryOp {

};

enum class BinaryOp {
    
};

enum class TernaryOp {

};