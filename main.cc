#include <print>
#include <execution>
#include <chrono>

#include "core/tensor.h"
#include "core/buffer.h"
#include "core/utils.h"

using namespace std::chrono;

int main() {
    auto t = Tensor<int>::zeros({2048, 2048});

    

}
